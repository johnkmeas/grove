<{{ block.settings.heading_tag | default: 'h2' }}
  class="grove-heading grove-heading--{{ block.settings.size | default: 'md' }}"
  {{ block.shopify_attributes }}
>
  {{ block.settings.text }}
</{{ block.settings.heading_tag | default: 'h2' }}>

<!-- SCHEMA_INJECT -->
